name: CI
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: npm ci
        working-directory: ui
      - name: Run ESLint
        run: npm run lint
        working-directory: ui
